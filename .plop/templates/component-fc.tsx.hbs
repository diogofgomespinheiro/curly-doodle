import * as React from 'react';

import { {{ pascalCase name }}Props } from './{{ kebabCase name}}.types';
import * as S from './{{ kebabCase name}}.styles';

const {{ pascalCase name }}: React.FC<{{ pascalCase name }}Props> = (props): React.ReactElement => {
  return <div></div>
};

export { {{ pascalCase name }} }

