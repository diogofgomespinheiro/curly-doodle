import * as React from 'react';
import { ComponentStory, ComponentMeta } from '@storybook/react';

import { {{ pascalCase name }} } from './{{kebabCase name}}';

export default {
  title: '{{ pascalCase type }}/{{ pascalCase name }}',
  component: {{ pascalCase name }},
} as ComponentMeta<typeof {{ pascalCase name }}>;

const Template: ComponentStory<typeof {{ pascalCase name }}> = (args) => <{{ pascalCase name }} {...args} />;

export const Default: ComponentStory<typeof {{ pascalCase name }}> = Template.bind({});
Default.args = {};
